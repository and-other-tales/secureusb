# SecureUSB Dependencies

# USB device monitoring
pyudev>=0.24.0

# TOTP authentication
pyotp>=2.9.0

# QR code generation for TOTP setup
qrcode[pil]>=7.4.0

# Image processing (for QR codes)
Pillow>=11.0.0

# Cryptography for secure storage
cryptography>=41.0.0

# GTK/GNOME support (should be system-installed via apt)
# PyGObject>=3.50.0  # Install via: apt install python3-gi
# gi.repository.Gtk  # Install via: apt install gir1.2-gtk-4.0

# D-Bus support (should be system-installed via apt)
# dbus-python>=1.3.0  # Install via: apt install python3-dbus
